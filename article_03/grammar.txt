program
  : expr+

expr
  : binary_expr
  | atom

binary_expr
  : term ((PLUS | MINUS) term)*

term
  : factor ((MUL | DIV | MOD) factor)*

atom
  : number
  | '(' expr ')'

number
  : digit+
  | digit+ '.' digit+

digit
  : [0-9]

PLUS
  : '+'

MINUS
  : '-'

MUL
  : '*'

DIV
  : '/'

MOD
  : '%'
